import { TransferActLayout } from '@/components/transfer-act/transfer-act-layout/transfer-act-layout.ui';

const TransferActsPage = () => {
  return <TransferActLayout />;
};

export default TransferActsPage;
